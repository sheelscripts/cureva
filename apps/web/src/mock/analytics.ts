// Re-exports mock analytics data from the SDK mock layer
import { 
  clinicOverview, 
  revenueMetrics, 
  slotSaverMetricsAdmin, 
  doctorMetrics, 
  agentMetrics, 
  promptMetrics, 
  escalations 
} from "@cureva/sdk";

export { 
  clinicOverview, 
  revenueMetrics, 
  slotSaverMetricsAdmin, 
  doctorMetrics, 
  agentMetrics, 
  promptMetrics, 
  escalations 
};
